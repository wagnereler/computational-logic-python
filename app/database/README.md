# Database

Responsabilidade: conexão SQLite e esquema inicial. Pode conter `connect`, pragmas e `CREATE TABLE`. Não deve validar preço, imprimir mensagens nem receber entrada do usuário. Depende da biblioteca padrão SQLite e Path. Correto: `initialize` cria tabelas sem dados artificiais. Incorreto: rejeitar nome duplicado com mensagem de interface aqui.
